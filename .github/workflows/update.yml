name: Update Anime Database

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-db:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ✅ PYTHONPATH
      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=${{ github.workspace }}" >> $GITHUB_ENV

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Set environment variables
        run: |
          echo "TMDB_TOKEN_1=${{ secrets.TMDB_TOKEN_1 }}" >> $GITHUB_ENV
          echo "TMDB_TOKEN_2=${{ secrets.TMDB_TOKEN_2 }}" >> $GITHUB_ENV
          echo "TMDB_TOKEN_3=${{ secrets.TMDB_TOKEN_3 }}" >> $GITHUB_ENV
          echo "TMDB_TOKEN_4=${{ secrets.TMDB_TOKEN_4 }}" >> $GITHUB_ENV
          echo "TMDB_TOKEN_5=${{ secrets.TMDB_TOKEN_5 }}" >> $GITHUB_ENV

      # 1️⃣ FETCH
      - name: Fetch AniList
        run: python scripts/fetch_anilist.py

      # 🐞 DEBUG — confirma que o arquivo foi criado
      - name: Debug data folder (after fetch)
        run: ls -R data

      # 2️⃣ MAP + VALIDATE BASE SCHEMA
      - name: Map & Validate Schema
        run: python scripts/mapper.py

      # 3️⃣ NORMALIZE TITLES
      - name: Normalize titles
        run: python scripts/normalize_titles.py

      # 4️⃣ MATCH TMDB
      - name: Match TMDB
        run: python scripts/match_tmdb.py

      # 5️⃣ ENRICH TMDB
      - name: Enrich TMDB
        run: python scripts/enrich_tmdb.py

      # 6️⃣ EXPORT FINAL
      - name: Export JSON
        run: python scripts/export_json.py

      # 🐞 DEBUG FINAL
      - name: Debug data folder (final)
        run: ls -R data

      # 7️⃣ COMMIT
      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          if ! git diff --quiet; then
            git add data/
            git commit -m "chore: update anime database"
            git push
          else
            echo "Nenhuma alteração detectada"
          fi
